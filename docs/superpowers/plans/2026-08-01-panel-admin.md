# Panel de Administración — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a protected `/admin` area where c3sar64 and Sarah can log in (password or Google) and manage the product catalog (create/edit/delete products, upload photos to Supabase Storage), closing the current unauthenticated-write security hole.

**Architecture:** Extend the existing ASP.NET Core backend with a `Usuarios` table, JWT-based auth (password login + Google ID token verification, both issuing the same JWT), an `[Authorize]`-protected image upload endpoint that proxies to Supabase Storage (S3-compatible), and `[Authorize]` on the existing product write endpoints. Extend the existing React frontend with `/admin/*` routes gated by a `ProtectedRoute`, reusing the existing Axios client, Tailwind palette, and Context pattern (`CartContext` → `AuthContext`).

**Tech Stack:** ASP.NET Core 8 / EF Core / Npgsql (existing), BCrypt.Net-Next, Microsoft.AspNetCore.Authentication.JwtBearer, Google.Apis.Auth, AWSSDK.S3 — React 18 / Vite / TypeScript / Tailwind / react-router-dom (existing), @react-oauth/google.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-08-01-panel-admin-design.md` — read it before starting if anything below is unclear.
- No automated test framework exists anywhere in this repo (backend or frontend). Do not introduce one for this feature — verify each task manually (build success, `curl`, or manual browser check) as specified in each task's steps.
- Secrets (S3 keys, JWT secret, DB connection string) go **only** in `backend-dotnet/appsettings.Development.json` (already gitignored) for local dev, and in Railway environment variables for production. Never in `appsettings.json`, `appsettings.Development.json.example`, or any frontend file.
- The Google OAuth Client ID is **not** secret and goes in `frontend/.env` (gitignored) as `VITE_GOOGLE_CLIENT_ID`, and later in Cloudflare Pages env vars for production.
- Category management stays out of scope — the admin UI only ever reads categories via the existing `GET /api/categorias`, never writes them.
- Follow existing code conventions: Spanish names for models/fields/routes (e.g. `Nombre`, `Precio`, `/api/productos`), controllers bind directly to EF entities (no DTO layer currently exists — don't introduce one).

---

## Backend

### Task 1: Instalar paquetes NuGet

**Files:**
- Modify: `backend-dotnet/Backend.csproj`

**Interfaces:**
- Produces: `BCrypt.Net.BCrypt` (hashing), `Microsoft.AspNetCore.Authentication.JwtBearer` middleware, `Google.Apis.Auth.GoogleJsonWebSignature`, `Amazon.S3.IAmazonS3` — used by every later backend task.

- [ ] **Step 1: Instalar los paquetes**

```bash
cd backend-dotnet
dotnet add package BCrypt.Net-Next
dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer
dotnet add package Google.Apis.Auth
dotnet add package AWSSDK.S3
```

- [ ] **Step 2: Verificar que restaura y compila**

Run: `dotnet build`
Expected: `Build succeeded.` sin errores.

- [ ] **Step 3: Commit**

```bash
git add backend-dotnet/Backend.csproj
git commit -m "Agrega paquetes NuGet para autenticación y subida a S3"
```

---

### Task 2: Modelo Usuario + migración de esquema

**Files:**
- Create: `backend-dotnet/Models/Usuario.cs`
- Modify: `backend-dotnet/Data/ApplicationDbContext.cs`
- Create: migration files under `backend-dotnet/Migrations/` (generated by EF tooling)

**Interfaces:**
- Produces: `Usuario` class with `Id (int)`, `Nombre (string)`, `Email (string)`, `PasswordHash (string)`, `IntentosFallidos (int)`, `Bloqueado (bool)`, `CreadoEn (DateTime)`. `ApplicationDbContext.Usuarios (DbSet<Usuario>)`. Used by Tasks 5, 7, 8, 9.

- [ ] **Step 1: Crear el modelo**

```csharp
// backend-dotnet/Models/Usuario.cs
using System.ComponentModel.DataAnnotations;

namespace Backend.Models
{
    public class Usuario
    {
        public int Id { get; set; }

        [Required]
        [MaxLength(200)]
        public string Nombre { get; set; } = string.Empty;

        [Required]
        [MaxLength(200)]
        public string Email { get; set; } = string.Empty;

        [Required]
        public string PasswordHash { get; set; } = string.Empty;

        public int IntentosFallidos { get; set; } = 0;

        public bool Bloqueado { get; set; } = false;

        public DateTime CreadoEn { get; set; } = DateTime.UtcNow;
    }
}
```

- [ ] **Step 2: Registrar el DbSet en el contexto**

En `backend-dotnet/Data/ApplicationDbContext.cs`, agregar el DbSet junto a los existentes:

```csharp
public DbSet<Categoria> Categorias => Set<Categoria>();
public DbSet<Producto> Productos => Set<Producto>();
public DbSet<MensajeContacto> MensajesContacto => Set<MensajeContacto>();
public DbSet<Usuario> Usuarios => Set<Usuario>();
```

Y en `OnModelCreating`, después del bloque de `MensajeContacto`, agregar:

```csharp
modelBuilder.Entity<Usuario>(entity =>
{
    entity.ToTable("usuarios");
    entity.HasIndex(u => u.Email).IsUnique();
});
```

- [ ] **Step 3: Generar la migración**

```bash
cd backend-dotnet
dotnet ef migrations add AddUsuarios
```

Expected: se crean archivos nuevos en `Migrations/` (`..._AddUsuarios.cs`, `.Designer.cs`) y se actualiza `ApplicationDbContextModelSnapshot.cs`.

- [ ] **Step 4: Verificar que compila**

Run: `dotnet build`
Expected: `Build succeeded.`

- [ ] **Step 5: Commit**

```bash
git add backend-dotnet/Models/Usuario.cs backend-dotnet/Data/ApplicationDbContext.cs backend-dotnet/Migrations/
git commit -m "Agrega modelo Usuario y migración de esquema"
```

---

### Task 3: Configuración (Jwt, Google, Supabase S3, AdminSeed)

**Files:**
- Modify: `backend-dotnet/appsettings.json`
- Modify: `backend-dotnet/appsettings.Development.json.example`
- Modify: `backend-dotnet/appsettings.Development.json` (gitignored — no se commitea)

**Interfaces:**
- Produces: config keys `Jwt:Secret`, `Google:ClientId`, `Supabase:S3:Endpoint|Region|Bucket|AccessKey|SecretKey`, `Supabase:PublicUrlBase`, `AdminSeed` (array) — consumidos por Tasks 5, 6, 7, 8, 10.

- [ ] **Step 1: Agregar las secciones vacías a `appsettings.json`** (valores reales van solo en Railway/local dev, nunca acá)

En `backend-dotnet/appsettings.json`, agregar después de `"Email": { ... }`:

```json
  "Jwt": {
    "Secret": ""
  },
  "Google": {
    "ClientId": ""
  },
  "Supabase": {
    "S3": {
      "Endpoint": "https://pglatlkxuhdzusdweqrk.storage.supabase.co/storage/v1/s3",
      "Region": "us-west-2",
      "Bucket": "media",
      "AccessKey": "",
      "SecretKey": ""
    },
    "PublicUrlBase": "https://pglatlkxuhdzusdweqrk.supabase.co/storage/v1/object/public"
  }
```

- [ ] **Step 2: Agregar la plantilla a `appsettings.Development.json.example`**

Agregar al final del JSON (antes del cierre `}`):

```json
  ,
  "Jwt": {
    "Secret": "GENERAR_UN_SECRETO_ALEATORIO_LARGO"
  },
  "Google": {
    "ClientId": "TU_GOOGLE_OAUTH_CLIENT_ID"
  },
  "Supabase": {
    "S3": {
      "AccessKey": "TU_ACCESS_KEY_ID",
      "SecretKey": "TU_SECRET_ACCESS_KEY"
    }
  },
  "AdminSeed": [
    { "Nombre": "Tu Nombre", "Email": "tu@email.com", "Password": "ElegiUnaContraseña" },
    { "Nombre": "Sarah", "Email": "sarah@email.com", "Password": "ElegiUnaContraseña" }
  ]
```

- [ ] **Step 3: Cargar valores reales en `appsettings.Development.json` (local, gitignored)**

Abrir `backend-dotnet/appsettings.Development.json` y agregar las mismas secciones con valores reales para poder probar localmente:

```json
  "Jwt": {
    "Secret": "REEMPLAZAR_CON_UN_SECRETO_ALEATORIO_LARGO_PROPIO"
  },
  "Google": {
    "ClientId": ""
  },
  "Supabase": {
    "S3": {
      "AccessKey": "REEMPLAZAR_CON_TU_ACCESS_KEY_DE_SUPABASE",
      "SecretKey": "REEMPLAZAR_CON_TU_SECRET_KEY_DE_SUPABASE"
    }
  },
  "AdminSeed": [
    { "Nombre": "Admin Prueba", "Email": "admin-local@test.com", "Password": "PruebaLocal123!" }
  ]
```

**Nota de seguridad:** los valores de `Jwt:Secret`, `Supabase:S3:AccessKey` y `Supabase:S3:SecretKey` que originalmente estaban escritos aquí como referencia fueron rotados y removidos de este documento tras la revisión final del panel de admin (2026-08-03) — nunca deben pegarse valores reales de secretos en un archivo versionado, ni siquiera en uno "de referencia" como este plan.

(`Google:ClientId` queda vacío hasta la Task 16, donde se crea el OAuth Client ID real — el login con Google no va a funcionar en local hasta entonces, pero el resto del panel sí.)

- [ ] **Step 4: Verificar que sigue sin trackearse por git**

Run: `git status`
Expected: `appsettings.Development.json` NO aparece en la lista (sigue ignorado). `appsettings.json` y `appsettings.Development.json.example` sí aparecen como modificados.

- [ ] **Step 5: Commit**

```bash
git add backend-dotnet/appsettings.json backend-dotnet/appsettings.Development.json.example
git commit -m "Agrega configuración de Jwt, Google OAuth y Supabase S3"
```

---

### Task 4: JwtService

**Files:**
- Create: `backend-dotnet/Services/IJwtService.cs`
- Create: `backend-dotnet/Services/JwtService.cs`

**Interfaces:**
- Consumes: `Usuario` (Task 2), config key `Jwt:Secret` (Task 3).
- Produces: `IJwtService.GenerateToken(Usuario usuario) : string` — usado por Tasks 7 y 8.

- [ ] **Step 1: Crear la interfaz**

```csharp
// backend-dotnet/Services/IJwtService.cs
using Backend.Models;

namespace Backend.Services
{
    public interface IJwtService
    {
        string GenerateToken(Usuario usuario);
    }
}
```

- [ ] **Step 2: Implementarla**

```csharp
// backend-dotnet/Services/JwtService.cs
using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;
using System.Text;
using Microsoft.IdentityModel.Tokens;
using Backend.Models;

namespace Backend.Services
{
    public class JwtService : IJwtService
    {
        private readonly string _secret;

        public JwtService(IConfiguration configuration)
        {
            _secret = configuration["Jwt:Secret"]
                ?? throw new InvalidOperationException("Falta configurar Jwt:Secret.");
        }

        public string GenerateToken(Usuario usuario)
        {
            var claims = new[]
            {
                new Claim(ClaimTypes.NameIdentifier, usuario.Id.ToString()),
                new Claim(ClaimTypes.Email, usuario.Email),
                new Claim(ClaimTypes.Name, usuario.Nombre),
            };

            var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_secret));
            var credentials = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);

            var token = new JwtSecurityToken(
                claims: claims,
                expires: DateTime.UtcNow.AddDays(7),
                signingCredentials: credentials
            );

            return new JwtSecurityTokenHandler().WriteToken(token);
        }
    }
}
```

- [ ] **Step 3: Verificar que compila**

Run: `dotnet build`
Expected: `Build succeeded.`

- [ ] **Step 4: Commit**

```bash
git add backend-dotnet/Services/IJwtService.cs backend-dotnet/Services/JwtService.cs
git commit -m "Agrega JwtService para emitir tokens de sesión"
```

---

### Task 5: AdminSeeder (carga las 2 cuentas admin al iniciar)

**Files:**
- Create: `backend-dotnet/Services/AdminSeeder.cs`
- Modify: `backend-dotnet/Program.cs`

**Interfaces:**
- Consumes: `ApplicationDbContext` (existente), config key `AdminSeed` (Task 3), `Usuario` (Task 2).
- Produces: `AdminSeeder.SeedAsync(ApplicationDbContext db, IConfiguration configuration) : Task` — llamado una vez al iniciar la app.

- [ ] **Step 1: Crear el seeder**

```csharp
// backend-dotnet/Services/AdminSeeder.cs
using Microsoft.EntityFrameworkCore;
using Backend.Data;
using Backend.Models;

namespace Backend.Services
{
    public static class AdminSeeder
    {
        public class AdminSeedEntry
        {
            public string Nombre { get; set; } = string.Empty;
            public string Email { get; set; } = string.Empty;
            public string Password { get; set; } = string.Empty;
        }

        public static async Task SeedAsync(ApplicationDbContext db, IConfiguration configuration)
        {
            var entries = configuration.GetSection("AdminSeed").Get<List<AdminSeedEntry>>() ?? new();

            foreach (var entry in entries)
            {
                if (string.IsNullOrWhiteSpace(entry.Email) || string.IsNullOrWhiteSpace(entry.Password))
                    continue;

                var hash = BCrypt.Net.BCrypt.HashPassword(entry.Password);
                var usuario = await db.Usuarios.FirstOrDefaultAsync(u => u.Email == entry.Email);

                if (usuario == null)
                {
                    db.Usuarios.Add(new Usuario
                    {
                        Nombre = entry.Nombre,
                        Email = entry.Email,
                        PasswordHash = hash,
                        IntentosFallidos = 0,
                        Bloqueado = false,
                        CreadoEn = DateTime.UtcNow,
                    });
                }
                else
                {
                    // Actualiza nombre/contraseña si cambiaron en la config, pero NO toca
                    // IntentosFallidos/Bloqueado — eso es estado de runtime, no de seed.
                    usuario.Nombre = entry.Nombre;
                    usuario.PasswordHash = hash;
                }
            }

            await db.SaveChangesAsync();
        }
    }
}
```

- [ ] **Step 2: Llamarlo desde `Program.cs`**

En `backend-dotnet/Program.cs`, dentro del bloque que ya existe:

```csharp
using (var scope = app.Services.CreateScope())
{
    var db = scope.ServiceProvider.GetRequiredService<ApplicationDbContext>();
    db.Database.Migrate();
}
```

Reemplazar por:

```csharp
using (var scope = app.Services.CreateScope())
{
    var db = scope.ServiceProvider.GetRequiredService<ApplicationDbContext>();
    db.Database.Migrate();
    await AdminSeeder.SeedAsync(db, app.Configuration);
}
```

- [ ] **Step 3: Correr localmente y verificar que se creó el usuario de prueba**

```bash
cd backend-dotnet
dotnet run
```

En otra terminal, mientras corre:

```bash
curl -s http://localhost:8002/api/categorias
```

Expected: responde con las categorías (confirma que arrancó bien y migró/seedeó sin errores en la consola de `dotnet run`).

Parar el servidor (Ctrl+C).

- [ ] **Step 4: Commit**

```bash
git add backend-dotnet/Services/AdminSeeder.cs backend-dotnet/Program.cs
git commit -m "Agrega AdminSeeder para cargar las cuentas admin al iniciar"
```

---

### Task 6: Middleware de autenticación JWT

**Files:**
- Modify: `backend-dotnet/Program.cs`

**Interfaces:**
- Consumes: config key `Jwt:Secret` (Task 3).
- Produces: pipeline de autenticación JWT activo — habilita `[Authorize]` en Tasks 7, 8, 9, 11.

- [ ] **Step 1: Agregar los `using` necesarios**

Al principio de `backend-dotnet/Program.cs`, agregar:

```csharp
using Microsoft.AspNetCore.Authentication.JwtBearer;
using Microsoft.IdentityModel.Tokens;
using System.Text;
```

- [ ] **Step 2: Registrar los servicios de autenticación**

Después del bloque de CORS (`builder.Services.AddCors(...)`) y antes del bloque de Email, agregar:

```csharp
// Autenticación JWT (login de administradores)
var jwtSecret = builder.Configuration["Jwt:Secret"]
    ?? throw new InvalidOperationException("Falta configurar Jwt:Secret.");

builder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
    .AddJwtBearer(options =>
    {
        options.TokenValidationParameters = new TokenValidationParameters
        {
            ValidateIssuer = false,
            ValidateAudience = false,
            ValidateLifetime = true,
            ValidateIssuerSigningKey = true,
            IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(jwtSecret)),
        };
    });

builder.Services.AddAuthorization();
builder.Services.AddScoped<IJwtService, JwtService>();
```

- [ ] **Step 3: Activar el middleware en el pipeline**

Ubicar:

```csharp
app.UseCors(FrontendCorsPolicy);

app.UseAuthorization();
```

Reemplazar por (agrega `UseAuthentication` antes de `UseAuthorization`, orden obligatorio):

```csharp
app.UseCors(FrontendCorsPolicy);

app.UseAuthentication();
app.UseAuthorization();
```

- [ ] **Step 4: Verificar que compila y arranca**

```bash
cd backend-dotnet
dotnet build
```

Expected: `Build succeeded.`

- [ ] **Step 5: Commit**

```bash
git add backend-dotnet/Program.cs
git commit -m "Activa middleware de autenticación JWT"
```

---

### Task 7: AuthController — login con contraseña (con bloqueo a los 3 intentos)

**Files:**
- Create: `backend-dotnet/Controllers/AuthController.cs`

**Interfaces:**
- Consumes: `ApplicationDbContext.Usuarios`, `IJwtService.GenerateToken` (Task 4).
- Produces: `POST /api/auth/login`, records `LoginRequest(string Email, string Password)`, `AuthResponse(string Token, string Nombre, string Email)` — `AuthResponse` reutilizado por Task 8.

- [ ] **Step 1: Crear el controller con el endpoint de login**

```csharp
// backend-dotnet/Controllers/AuthController.cs
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;
using Backend.Data;
using Backend.Services;

namespace Backend.Controllers
{
    [ApiController]
    [Route("api/auth")]
    public class AuthController : ControllerBase
    {
        private readonly ApplicationDbContext _context;
        private readonly IJwtService _jwtService;

        public AuthController(ApplicationDbContext context, IJwtService jwtService)
        {
            _context = context;
            _jwtService = jwtService;
        }

        public record LoginRequest(string Email, string Password);
        public record AuthResponse(string Token, string Nombre, string Email);

        private const int MaxIntentosFallidos = 3;

        [HttpPost("login")]
        public async Task<ActionResult<AuthResponse>> Login(LoginRequest request)
        {
            var usuario = await _context.Usuarios.FirstOrDefaultAsync(u => u.Email == request.Email);
            if (usuario == null)
                return Unauthorized(new { message = "Credenciales inválidas." });

            if (usuario.Bloqueado)
            {
                return Unauthorized(new
                {
                    message = "Esta cuenta está bloqueada por intentos fallidos. Iniciá sesión con Google para desbloquearla."
                });
            }

            if (!BCrypt.Net.BCrypt.Verify(request.Password, usuario.PasswordHash))
            {
                usuario.IntentosFallidos++;
                if (usuario.IntentosFallidos >= MaxIntentosFallidos)
                    usuario.Bloqueado = true;

                await _context.SaveChangesAsync();
                return Unauthorized(new { message = "Credenciales inválidas." });
            }

            usuario.IntentosFallidos = 0;
            await _context.SaveChangesAsync();

            var token = _jwtService.GenerateToken(usuario);
            return new AuthResponse(token, usuario.Nombre, usuario.Email);
        }
    }
}
```

- [ ] **Step 2: Verificar manualmente el flujo completo (login correcto, incorrecto, bloqueo)**

```bash
cd backend-dotnet
dotnet run
```

En otra terminal:

```bash
# 1) Credenciales correctas -> 200 con token
curl -s -w "\n%{http_code}\n" -X POST http://localhost:8002/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin-local@test.com","password":"PruebaLocal123!"}'

# 2) Credenciales incorrectas -> 401 (repetir 3 veces para bloquear)
curl -s -w "\n%{http_code}\n" -X POST http://localhost:8002/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin-local@test.com","password":"mal"}'
```

Expected: (1) devuelve `200` con un JSON `{"token": "...", "nombre": "Admin Prueba", "email": "admin-local@test.com"}`. (2) devuelve `401` con `{"message":"Credenciales inválidas."}`; repetido una 3ra vez, el mensaje pasa a indicar que la cuenta está bloqueada, y un intento posterior con la contraseña CORRECTA también debe devolver 401 con el mensaje de bloqueo (probarlo).

Después de probar el bloqueo, resetealo para no arrastrar el estado a la próxima tarea:

```bash
# usando psql, o el Table Editor de Supabase: UPDATE usuarios SET intentos_fallidos = 0, bloqueado = false WHERE email = 'admin-local@test.com';
```

Parar el servidor (Ctrl+C).

- [ ] **Step 3: Commit**

```bash
git add backend-dotnet/Controllers/AuthController.cs
git commit -m "Agrega login con contraseña y bloqueo a los 3 intentos fallidos"
```

---

### Task 8: AuthController — login con Google

**Files:**
- Modify: `backend-dotnet/Controllers/AuthController.cs`

**Interfaces:**
- Consumes: `AuthResponse` record (Task 7), config key `Google:ClientId` (Task 3), `Google.Apis.Auth.GoogleJsonWebSignature`.
- Produces: `POST /api/auth/google`.

- [ ] **Step 1: Agregar los `using` y el constructor con `IConfiguration`**

En `backend-dotnet/Controllers/AuthController.cs`, agregar arriba:

```csharp
using Google.Apis.Auth;
```

Modificar el constructor para recibir también `IConfiguration`:

```csharp
private readonly ApplicationDbContext _context;
private readonly IJwtService _jwtService;
private readonly IConfiguration _configuration;

public AuthController(ApplicationDbContext context, IJwtService jwtService, IConfiguration configuration)
{
    _context = context;
    _jwtService = jwtService;
    _configuration = configuration;
}
```

- [ ] **Step 2: Agregar el endpoint**

Dentro de la clase, después del método `Login`:

```csharp
public record GoogleLoginRequest(string IdToken);

[HttpPost("google")]
public async Task<ActionResult<AuthResponse>> GoogleLogin(GoogleLoginRequest request)
{
    var clientId = _configuration["Google:ClientId"];
    if (string.IsNullOrWhiteSpace(clientId))
        return StatusCode(500, new { message = "Login con Google no está configurado." });

    GoogleJsonWebSignature.Payload payload;
    try
    {
        payload = await GoogleJsonWebSignature.ValidateAsync(request.IdToken, new GoogleJsonWebSignature.ValidationSettings
        {
            Audience = new[] { clientId },
        });
    }
    catch (InvalidJwtException)
    {
        return Unauthorized(new { message = "Token de Google inválido." });
    }

    var usuario = await _context.Usuarios.FirstOrDefaultAsync(u => u.Email == payload.Email);
    if (usuario == null)
        return StatusCode(403, new { message = "Esta cuenta no tiene acceso al panel." });

    // Login exitoso con Google desbloquea la cuenta (ver spec: auto-desbloqueo).
    usuario.IntentosFallidos = 0;
    usuario.Bloqueado = false;
    await _context.SaveChangesAsync();

    var token = _jwtService.GenerateToken(usuario);
    return new AuthResponse(token, usuario.Nombre, usuario.Email);
}
```

- [ ] **Step 3: Verificar que compila**

Run: `dotnet build`
Expected: `Build succeeded.` (el endpoint no se puede probar end-to-end todavía porque falta `Google:ClientId` real — eso se completa en la Task 16. Por ahora alcanza con que compile y que, sin `ClientId` configurado, devuelva 500 en vez de explotar.)

```bash
cd backend-dotnet
dotnet run
```

```bash
curl -s -w "\n%{http_code}\n" -X POST http://localhost:8002/api/auth/google \
  -H "Content-Type: application/json" \
  -d '{"idToken":"cualquiera"}'
```

Expected: `500` con `{"message":"Login con Google no está configurado."}` (porque `Google:ClientId` sigue vacío en local). Parar el servidor.

- [ ] **Step 4: Commit**

```bash
git add backend-dotnet/Controllers/AuthController.cs
git commit -m "Agrega login con Google al AuthController"
```

---

### Task 9: Proteger endpoints de escritura de productos

**Files:**
- Modify: `backend-dotnet/Controllers/ProductosController.cs`

**Interfaces:**
- Consumes: middleware de autenticación (Task 6).
- Produces: `POST/PUT/DELETE /api/productos` ahora requieren JWT válido; `GET` sigue público.

- [ ] **Step 1: Agregar `[Authorize]` a los 3 métodos de escritura**

En `backend-dotnet/Controllers/ProductosController.cs`, agregar el using:

```csharp
using Microsoft.AspNetCore.Authorization;
```

Y agregar el atributo justo arriba de cada uno de estos 3 métodos (sin tocar los `GET`):

```csharp
[Authorize]
[HttpPost]
public async Task<ActionResult<Producto>> PostProducto(Producto producto)
```

```csharp
[Authorize]
[HttpPut("{id:int}")]
public async Task<IActionResult> PutProducto(int id, Producto producto)
```

```csharp
[Authorize]
[HttpDelete("{id:int}")]
public async Task<IActionResult> DeleteProducto(int id)
```

- [ ] **Step 2: Verificar que ahora rechaza sin token y acepta con token**

```bash
cd backend-dotnet
dotnet run
```

```bash
# Sin token -> 401
curl -s -o /dev/null -w "%{http_code}\n" -X DELETE http://localhost:8002/api/productos/999999

# Con token válido (usar el token que devolvió /api/auth/login en la Task 7) -> 404 (no 401, porque el id no existe pero SÍ pasó la autorización)
TOKEN="pegar_aca_el_token_de_la_task_7"
curl -s -o /dev/null -w "%{http_code}\n" -X DELETE http://localhost:8002/api/productos/999999 \
  -H "Authorization: Bearer $TOKEN"

# GET sigue público -> 200
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8002/api/productos
```

Expected: primer curl `401`, segundo `404`, tercero `200`. Parar el servidor.

- [ ] **Step 3: Commit**

```bash
git add backend-dotnet/Controllers/ProductosController.cs
git commit -m "Protege los endpoints de escritura de productos con [Authorize]"
```

---

### Task 10: Cliente S3 + SupabaseStorageService

**Files:**
- Create: `backend-dotnet/Services/IStorageService.cs`
- Create: `backend-dotnet/Services/SupabaseStorageService.cs`
- Modify: `backend-dotnet/Program.cs`

**Interfaces:**
- Consumes: config keys `Supabase:S3:*` y `Supabase:PublicUrlBase` (Task 3).
- Produces: `IStorageService.SubirImagenAsync(IFormFile archivo, string carpeta) : Task<string>` — usado por Task 11.

- [ ] **Step 1: Crear la interfaz**

```csharp
// backend-dotnet/Services/IStorageService.cs
namespace Backend.Services
{
    public interface IStorageService
    {
        Task<string> SubirImagenAsync(IFormFile archivo, string carpeta);
    }
}
```

- [ ] **Step 2: Implementarla**

```csharp
// backend-dotnet/Services/SupabaseStorageService.cs
using Amazon.S3;
using Amazon.S3.Model;

namespace Backend.Services
{
    public class SupabaseStorageService : IStorageService
    {
        private readonly IAmazonS3 _s3;
        private readonly string _bucket;
        private readonly string _publicBaseUrl;

        public SupabaseStorageService(IAmazonS3 s3, IConfiguration configuration)
        {
            _s3 = s3;
            _bucket = configuration["Supabase:S3:Bucket"] ?? "media";
            _publicBaseUrl = configuration["Supabase:PublicUrlBase"]
                ?? throw new InvalidOperationException("Falta configurar Supabase:PublicUrlBase.");
        }

        public async Task<string> SubirImagenAsync(IFormFile archivo, string carpeta)
        {
            var extension = Path.GetExtension(archivo.FileName);
            var key = $"{carpeta}/{Guid.NewGuid()}{extension}";

            using var stream = archivo.OpenReadStream();
            await _s3.PutObjectAsync(new PutObjectRequest
            {
                BucketName = _bucket,
                Key = key,
                InputStream = stream,
                ContentType = archivo.ContentType,
            });

            return $"{_publicBaseUrl}/{_bucket}/{key}";
        }
    }
}
```

- [ ] **Step 3: Registrar el cliente S3 y el servicio en `Program.cs`**

Agregar el using al principio:

```csharp
using Amazon.S3;
```

Después del bloque de `builder.Services.AddScoped<IJwtService, JwtService>();` (agregado en la Task 6), agregar:

```csharp
// Cliente S3 apuntando a Supabase Storage
builder.Services.AddSingleton<IAmazonS3>(sp =>
{
    var config = sp.GetRequiredService<IConfiguration>();
    var s3Config = new AmazonS3Config
    {
        ServiceURL = config["Supabase:S3:Endpoint"],
        ForcePathStyle = true,
        AuthenticationRegion = config["Supabase:S3:Region"] ?? "us-west-2",
    };
    return new AmazonS3Client(
        config["Supabase:S3:AccessKey"],
        config["Supabase:S3:SecretKey"],
        s3Config);
});
builder.Services.AddScoped<IStorageService, SupabaseStorageService>();
```

- [ ] **Step 4: Verificar que compila**

Run: `dotnet build`
Expected: `Build succeeded.`

- [ ] **Step 5: Commit**

```bash
git add backend-dotnet/Services/IStorageService.cs backend-dotnet/Services/SupabaseStorageService.cs backend-dotnet/Program.cs
git commit -m "Agrega cliente S3 y servicio de subida a Supabase Storage"
```

---

### Task 11: UploadsController

**Files:**
- Create: `backend-dotnet/Controllers/UploadsController.cs`

**Interfaces:**
- Consumes: `IStorageService.SubirImagenAsync` (Task 10), middleware de autenticación (Task 6).
- Produces: `POST /api/uploads/imagen` (protegido), record `UploadResponse(string Url)`.

- [ ] **Step 1: Crear el controller**

```csharp
// backend-dotnet/Controllers/UploadsController.cs
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Backend.Services;

namespace Backend.Controllers
{
    [ApiController]
    [Route("api/uploads")]
    [Authorize]
    public class UploadsController : ControllerBase
    {
        private static readonly string[] TiposPermitidos = { "image/jpeg", "image/png", "image/webp" };
        private const long TamanoMaximoBytes = 5 * 1024 * 1024;

        private readonly IStorageService _storageService;

        public UploadsController(IStorageService storageService)
        {
            _storageService = storageService;
        }

        public record UploadResponse(string Url);

        [HttpPost("imagen")]
        public async Task<ActionResult<UploadResponse>> SubirImagen([FromForm] IFormFile archivo)
        {
            if (archivo == null || archivo.Length == 0)
                return BadRequest(new { message = "No se recibió ningún archivo." });

            if (!TiposPermitidos.Contains(archivo.ContentType))
                return StatusCode(415, new { message = "Formato no soportado. Usá JPG, PNG o WEBP." });

            if (archivo.Length > TamanoMaximoBytes)
                return StatusCode(413, new { message = "La imagen no puede pesar más de 5MB." });

            try
            {
                var url = await _storageService.SubirImagenAsync(archivo, "fotos/productos");
                return new UploadResponse(url);
            }
            catch (Exception)
            {
                return StatusCode(502, new { message = "No se pudo subir la imagen. Probá de nuevo." });
            }
        }
    }
}
```

- [ ] **Step 2: Verificar subiendo una imagen real**

```bash
cd backend-dotnet
dotnet run
```

En otra terminal (conseguir un `TOKEN` nuevo con `POST /api/auth/login` como en la Task 7):

```bash
TOKEN="pegar_aca_el_token"

curl -s -w "\n%{http_code}\n" -X POST http://localhost:8002/api/uploads/imagen \
  -H "Authorization: Bearer $TOKEN" \
  -F "archivo=@../frontend/public/productos/donas.png;type=image/png"
```

Expected: `200` con `{"url":"https://pglatlkxuhdzusdweqrk.supabase.co/storage/v1/object/public/media/fotos/productos/<guid>.png"}`. Abrir esa URL en el navegador y confirmar que se ve la foto de las donas (si da 403/404 en el navegador, el bucket `media` no está marcado como público en el dashboard de Supabase — Storage → media → Settings → Make public — corregirlo ahí antes de seguir).

También probar sin token:

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:8002/api/uploads/imagen \
  -F "archivo=@../frontend/public/productos/donas.png;type=image/png"
```

Expected: `401`. Parar el servidor.

- [ ] **Step 3: Commit**

```bash
git add backend-dotnet/Controllers/UploadsController.cs
git commit -m "Agrega endpoint protegido de subida de imágenes a Supabase Storage"
```

---

### Task 12: Migrar las 10 fotos existentes a Supabase Storage

**Files:**
- No se crean archivos de código — es una tarea de datos, usando los endpoints ya construidos.
- Modify (datos, no código): filas de la tabla `productos` en Supabase (campo `ImagenUrl`).

**Interfaces:**
- Consumes: `POST /api/uploads/imagen` (Task 11), `PUT /api/productos/{id}` (existente).

- [ ] **Step 1: Levantar el backend local**

```bash
cd backend-dotnet
dotnet run
```

- [ ] **Step 2: Conseguir un token**

```bash
TOKEN=$(curl -s -X POST http://localhost:8002/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin-local@test.com","password":"PruebaLocal123!"}' | python3 -c "import sys,json; print(json.load(sys.stdin)['token'])")
echo "$TOKEN"
```

Expected: imprime un JWT (string largo).

- [ ] **Step 3: Subir las 10 fotos y actualizar cada producto**

Mapeo confirmado (id de producto → archivo local):

| Id | Archivo |
|----|---------|
| 4  | tres-leches.png |
| 3  | queque.png |
| 2  | pie-de-limon.png |
| 8  | surtiditas.png |
| 7  | galletas.png |
| 6  | mini-alfajores.png |
| 5  | donas.png |
| 11 | mini-cunapes.png |
| 10 | mini-empanadas.png |
| 9  | chorizos-envueltos.png |

```bash
cd ..  # a la raíz del repo

declare -A FOTOS=(
  [4]=tres-leches.png
  [3]=queque.png
  [2]=pie-de-limon.png
  [8]=surtiditas.png
  [7]=galletas.png
  [6]=mini-alfajores.png
  [5]=donas.png
  [11]=mini-cunapes.png
  [10]=mini-empanadas.png
  [9]=chorizos-envueltos.png
)

for ID in "${!FOTOS[@]}"; do
  ARCHIVO="${FOTOS[$ID]}"
  echo "Subiendo $ARCHIVO para producto $ID..."

  URL=$(curl -s -X POST http://localhost:8002/api/uploads/imagen \
    -H "Authorization: Bearer $TOKEN" \
    -F "archivo=@frontend/public/productos/$ARCHIVO;type=image/png" \
    | python3 -c "import sys,json; print(json.load(sys.stdin)['url'])")

  echo "  -> $URL"

  PRODUCTO=$(curl -s http://localhost:8002/api/productos/$ID)

  python3 -c "
import json, urllib.request

producto = json.loads('''$PRODUCTO''')
producto['imagenUrl'] = '$URL'
producto.pop('categoria', None)

req = urllib.request.Request(
    'http://localhost:8002/api/productos/$ID',
    data=json.dumps(producto).encode(),
    headers={'Content-Type': 'application/json', 'Authorization': 'Bearer $TOKEN'},
    method='PUT',
)
resp = urllib.request.urlopen(req)
print('  PUT status:', resp.status)
"
done
```

- [ ] **Step 4: Verificar que los 10 productos apuntan a Supabase**

```bash
curl -s http://localhost:8002/api/productos | python3 -c "
import sys, json
productos = json.load(sys.stdin)
for p in productos:
    print(p['id'], p['nombre'], '->', p['imagenUrl'])
"
```

Expected: los 10 `imagenUrl` empiezan con `https://pglatlkxuhdzusdweqrk.supabase.co/storage/v1/object/public/media/fotos/productos/`.

Parar el servidor.

- [ ] **Step 5: Borrar las fotos locales, ya no se usan**

```bash
git rm -r frontend/public/productos
```

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "Migra las fotos de producto a Supabase Storage"
```

---

## Frontend

### Task 13: AuthContext + api/auth.ts

**Files:**
- Create: `frontend/src/context/AuthContext.tsx`
- Create: `frontend/src/api/auth.ts`

**Interfaces:**
- Produces: `AuthProvider` component, `useAuth()` hook returning `{ token: string | null, usuario: { nombre: string; email: string } | null, isAuthenticated: boolean, login(token: string, usuario: { nombre: string; email: string }): void, logout(): void }`. `login(email, password): Promise<AuthResponse>`, `loginWithGoogle(idToken): Promise<AuthResponse>`, `interface AuthResponse { token: string; nombre: string; email: string }`. Usado por Tasks 14, 15, 17, 18.

- [ ] **Step 1: Crear `api/auth.ts`**

```typescript
// frontend/src/api/auth.ts
import { api } from './client'

export interface AuthResponse {
  token: string
  nombre: string
  email: string
}

export async function login(email: string, password: string): Promise<AuthResponse> {
  const { data } = await api.post<AuthResponse>('/auth/login', { email, password })
  return data
}

export async function loginWithGoogle(idToken: string): Promise<AuthResponse> {
  const { data } = await api.post<AuthResponse>('/auth/google', { idToken })
  return data
}
```

- [ ] **Step 2: Crear `context/AuthContext.tsx`**

```typescript
// frontend/src/context/AuthContext.tsx
import { createContext, useContext, useState, type ReactNode } from 'react'

const TOKEN_KEY = 'sarah_admin_token'
const USER_KEY = 'sarah_admin_usuario'

export interface AdminUsuario {
  nombre: string
  email: string
}

interface AuthContextValue {
  token: string | null
  usuario: AdminUsuario | null
  isAuthenticated: boolean
  login: (token: string, usuario: AdminUsuario) => void
  logout: () => void
}

const AuthContext = createContext<AuthContextValue | undefined>(undefined)

function loadUsuario(): AdminUsuario | null {
  try {
    const raw = localStorage.getItem(USER_KEY)
    return raw ? (JSON.parse(raw) as AdminUsuario) : null
  } catch {
    return null
  }
}

export function AuthProvider({ children }: { children: ReactNode }) {
  const [token, setToken] = useState<string | null>(() => localStorage.getItem(TOKEN_KEY))
  const [usuario, setUsuario] = useState<AdminUsuario | null>(loadUsuario)

  const login = (newToken: string, newUsuario: AdminUsuario) => {
    localStorage.setItem(TOKEN_KEY, newToken)
    localStorage.setItem(USER_KEY, JSON.stringify(newUsuario))
    setToken(newToken)
    setUsuario(newUsuario)
  }

  const logout = () => {
    localStorage.removeItem(TOKEN_KEY)
    localStorage.removeItem(USER_KEY)
    setToken(null)
    setUsuario(null)
  }

  return (
    <AuthContext.Provider value={{ token, usuario, isAuthenticated: !!token, login, logout }}>
      {children}
    </AuthContext.Provider>
  )
}

export function useAuth() {
  const ctx = useContext(AuthContext)
  if (!ctx) throw new Error('useAuth debe usarse dentro de un AuthProvider')
  return ctx
}
```

- [ ] **Step 3: Verificar que compila**

Run: `cd frontend && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/context/AuthContext.tsx frontend/src/api/auth.ts
git commit -m "Agrega AuthContext y cliente de autenticación"
```

---

### Task 14: Interceptores de Axios (token + manejo de 401)

**Files:**
- Modify: `frontend/src/api/client.ts`

**Interfaces:**
- Consumes: `localStorage` key `sarah_admin_token` (mismo nombre que en Task 13).
- Produces: toda request hecha con `api` adjunta el header `Authorization` automáticamente si hay sesión, y ante un 401 limpia la sesión y redirige.

- [ ] **Step 1: Modificar `client.ts`**

Reemplazar todo el contenido de `frontend/src/api/client.ts` por:

```typescript
import axios from 'axios'

const API_URL = import.meta.env.VITE_API_URL ?? 'http://localhost:8000/api'

export const api = axios.create({
  baseURL: API_URL,
  headers: {
    'Content-Type': 'application/json',
  },
})

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('sarah_admin_token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('sarah_admin_token')
      localStorage.removeItem('sarah_admin_usuario')
      if (window.location.pathname.startsWith('/admin')) {
        window.location.href = '/admin/login'
      }
    }
    return Promise.reject(error)
  },
)
```

- [ ] **Step 2: Verificar que compila**

Run: `cd frontend && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/api/client.ts
git commit -m "Agrega interceptores de Axios para token y expiración de sesión"
```

---

### Task 15: ProtectedRoute

**Files:**
- Create: `frontend/src/components/admin/ProtectedRoute.tsx`

**Interfaces:**
- Consumes: `useAuth()` (Task 13).
- Produces: componente `ProtectedRoute` usado como wrapper de rutas en Task 23.

- [ ] **Step 1: Crear el componente**

```typescript
// frontend/src/components/admin/ProtectedRoute.tsx
import { Navigate, Outlet } from 'react-router-dom'
import { useAuth } from '../../context/AuthContext'

export default function ProtectedRoute() {
  const { isAuthenticated } = useAuth()

  if (!isAuthenticated) {
    return <Navigate to="/admin/login" replace />
  }

  return <Outlet />
}
```

- [ ] **Step 2: Verificar que compila**

Run: `cd frontend && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/admin/ProtectedRoute.tsx
git commit -m "Agrega ProtectedRoute para las rutas de admin"
```

---

### Task 16: Google Identity Services (paquete + Client ID)

**Files:**
- Modify: `frontend/package.json` (vía npm)
- Create: `frontend/.env.example`
- Modify: `frontend/.env` (gitignored)

**Interfaces:**
- Produces: `VITE_GOOGLE_CLIENT_ID` disponible vía `import.meta.env`, paquete `@react-oauth/google` (`GoogleOAuthProvider`, `GoogleLogin`) — usado por Tasks 18 y 17.

**⚠️ Requiere acción manual tuya (no la puede hacer un agente sin acceso a tu cuenta de Google):**

- [ ] **Step 1: Crear el OAuth Client ID en Google Cloud Console**

1. Ir a https://console.cloud.google.com/apis/credentials
2. Crear un proyecto nuevo (o usar uno existente).
3. "Configure OAuth consent screen" → tipo "External" → completar nombre de la app ("Panel Sarah") y tu email → guardar.
4. "Create Credentials" → "OAuth client ID" → tipo "Web application".
5. En "Authorized JavaScript origins" agregar:
   - `http://localhost:5173` (dev local)
   - `https://web-sarah.pages.dev` (producción)
6. Crear, y copiar el "Client ID" generado (termina en `.apps.googleusercontent.com`).

- [ ] **Step 2: Instalar el paquete**

```bash
cd frontend
npm install @react-oauth/google
```

- [ ] **Step 3: Crear `.env.example`**

```bash
# frontend/.env.example
VITE_API_URL=http://localhost:8002/api
VITE_GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com
```

- [ ] **Step 4: Agregar el Client ID real a `.env` (local, gitignored)**

Agregar la línea al `frontend/.env` existente:

```
VITE_GOOGLE_CLIENT_ID=<pegar el Client ID del Step 1>
```

- [ ] **Step 5: Verificar que sigue ignorado por git**

Run: `git status`
Expected: `frontend/.env` NO aparece listado. `frontend/.env.example`, `frontend/package.json` y `frontend/package-lock.json` sí aparecen.

- [ ] **Step 6: Commit**

```bash
git add frontend/package.json frontend/package-lock.json frontend/.env.example
git commit -m "Agrega @react-oauth/google y configuración de Google Client ID"
```

---

### Task 17: AdminLogin

**Files:**
- Create: `frontend/src/pages/admin/AdminLogin.tsx`

**Interfaces:**
- Consumes: `useAuth()` (Task 13), `login`/`loginWithGoogle` de `api/auth.ts` (Task 13), `GoogleLogin` de `@react-oauth/google` (Task 16).
- Produces: componente `AdminLogin`, usado en Task 23.

- [ ] **Step 1: Crear la página**

```typescript
// frontend/src/pages/admin/AdminLogin.tsx
import { useState, type FormEvent } from 'react'
import { useNavigate } from 'react-router-dom'
import { GoogleLogin, type CredentialResponse } from '@react-oauth/google'
import { useAuth } from '../../context/AuthContext'
import { login as loginRequest, loginWithGoogle } from '../../api/auth'

export default function AdminLogin() {
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)
  const { login } = useAuth()
  const navigate = useNavigate()

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault()
    setError(null)
    setLoading(true)
    try {
      const res = await loginRequest(email, password)
      login(res.token, { nombre: res.nombre, email: res.email })
      navigate('/admin/productos')
    } catch {
      setError('Credenciales inválidas.')
    } finally {
      setLoading(false)
    }
  }

  const handleGoogleSuccess = async (credentialResponse: CredentialResponse) => {
    if (!credentialResponse.credential) return
    setError(null)
    try {
      const res = await loginWithGoogle(credentialResponse.credential)
      login(res.token, { nombre: res.nombre, email: res.email })
      navigate('/admin/productos')
    } catch {
      setError('Esta cuenta no tiene acceso al panel.')
    }
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-cream px-4">
      <div className="w-full max-w-sm bg-white rounded-2xl shadow-md p-8">
        <h1 className="text-2xl font-semibold text-mauve mb-6 text-center">Panel de Sarah</h1>

        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label className="block text-sm font-medium text-[#3D2C33] mb-1">Email</label>
            <input
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              required
              className="w-full rounded-lg border border-taupe/50 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-mauve"
            />
          </div>
          <div>
            <label className="block text-sm font-medium text-[#3D2C33] mb-1">Contraseña</label>
            <input
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              required
              className="w-full rounded-lg border border-taupe/50 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-mauve"
            />
          </div>

          {error && <p className="text-sm text-red-600">{error}</p>}

          <button
            type="submit"
            disabled={loading}
            className="w-full rounded-full bg-rose-metallic hover:bg-rose-metallic-dark text-white font-medium py-2.5 transition-colors disabled:opacity-60"
          >
            {loading ? 'Entrando…' : 'Entrar'}
          </button>
        </form>

        <div className="my-6 flex items-center gap-3">
          <div className="h-px flex-1 bg-taupe/40" />
          <span className="text-xs text-taupe">o</span>
          <div className="h-px flex-1 bg-taupe/40" />
        </div>

        <div className="flex justify-center">
          <GoogleLogin
            onSuccess={handleGoogleSuccess}
            onError={() => setError('No se pudo iniciar sesión con Google.')}
          />
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar que compila**

Run: `cd frontend && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/admin/AdminLogin.tsx
git commit -m "Agrega pantalla de login del panel de admin"
```

---

### Task 18: AdminLayout

**Files:**
- Create: `frontend/src/components/admin/AdminLayout.tsx`

**Interfaces:**
- Consumes: `useAuth()` (Task 13).
- Produces: componente `AdminLayout`, usado en Task 23.

- [ ] **Step 1: Crear el layout**

```typescript
// frontend/src/components/admin/AdminLayout.tsx
import { Outlet, Link } from 'react-router-dom'
import { useAuth } from '../../context/AuthContext'

export default function AdminLayout() {
  const { usuario, logout } = useAuth()

  return (
    <div className="min-h-screen bg-cream">
      <header className="bg-white border-b border-taupe/30 px-6 py-4 flex items-center justify-between">
        <Link to="/admin/productos" className="font-semibold text-mauve">
          Panel de Sarah
        </Link>
        <div className="flex items-center gap-4 text-sm text-[#3D2C33]">
          <span>{usuario?.nombre}</span>
          <button onClick={logout} className="text-mauve hover:underline">
            Cerrar sesión
          </button>
        </div>
      </header>
      <main className="max-w-5xl mx-auto px-6 py-8">
        <Outlet />
      </main>
    </div>
  )
}
```

- [ ] **Step 2: Verificar que compila**

Run: `cd frontend && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/admin/AdminLayout.tsx
git commit -m "Agrega layout del panel de admin"
```

---

### Task 19: api/products.ts — create/update/delete + tipo ProductInput

**Files:**
- Modify: `frontend/src/types/product.ts`
- Modify: `frontend/src/api/products.ts`

**Interfaces:**
- Produces: `interface ProductInput { nombre: string; descripcion: string | null; precio: number; imagenUrl: string | null; disponible: boolean; categoriaId: number | null }`, `createProduct(payload: ProductInput): Promise<Product>`, `updateProduct(id: number, payload: ProductInput): Promise<void>`, `deleteProduct(id: number): Promise<void>` — usados por Tasks 21 y 22.

- [ ] **Step 1: Agregar `ProductInput` a `types/product.ts`**

Agregar al final de `frontend/src/types/product.ts`:

```typescript
export interface ProductInput {
  nombre: string
  descripcion: string | null
  precio: number
  imagenUrl: string | null
  disponible: boolean
  categoriaId: number | null
}
```

- [ ] **Step 2: Agregar las funciones a `api/products.ts`**

Modificar el import y agregar al final de `frontend/src/api/products.ts`:

```typescript
import { api } from './client'
import type { Categoria, Product, ProductInput } from '../types/product'
```

```typescript
export async function createProduct(payload: ProductInput): Promise<Product> {
  const { data } = await api.post<Product>('/productos', payload)
  return data
}

export async function updateProduct(id: number, payload: ProductInput): Promise<void> {
  await api.put(`/productos/${id}`, { id, ...payload })
}

export async function deleteProduct(id: number): Promise<void> {
  await api.delete(`/productos/${id}`)
}
```

- [ ] **Step 3: Verificar que compila**

Run: `cd frontend && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/types/product.ts frontend/src/api/products.ts
git commit -m "Agrega funciones de crear/editar/eliminar producto"
```

---

### Task 20: api/uploads.ts

**Files:**
- Create: `frontend/src/api/uploads.ts`

**Interfaces:**
- Consumes: cliente `api` (Task 14, ya con interceptor de token).
- Produces: `uploadImagen(file: File): Promise<string>` — usado por Task 21.

- [ ] **Step 1: Crear el archivo**

```typescript
// frontend/src/api/uploads.ts
import { api } from './client'

export async function uploadImagen(file: File): Promise<string> {
  const formData = new FormData()
  formData.append('archivo', file)

  const { data } = await api.post<{ url: string }>('/uploads/imagen', formData, {
    headers: { 'Content-Type': 'multipart/form-data' },
  })

  return data.url
}
```

- [ ] **Step 2: Verificar que compila**

Run: `cd frontend && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/api/uploads.ts
git commit -m "Agrega cliente de subida de imágenes"
```

---

### Task 21: AdminProductoForm (crear/editar)

**Files:**
- Create: `frontend/src/pages/admin/AdminProductoForm.tsx`

**Interfaces:**
- Consumes: `getProduct`, `getCategorias`, `createProduct`, `updateProduct` (Task 19, y `getProduct`/`getCategorias` ya existentes), `uploadImagen` (Task 20), `ProductInput`/`Categoria` (Task 19 / existente).
- Produces: componente `AdminProductoForm`, usado en Task 23.

- [ ] **Step 1: Crear la página**

```typescript
// frontend/src/pages/admin/AdminProductoForm.tsx
import { useEffect, useState, type ChangeEvent, type FormEvent } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { getProduct, getCategorias, createProduct, updateProduct } from '../../api/products'
import { uploadImagen } from '../../api/uploads'
import type { Categoria, ProductInput } from '../../types/product'

const initialForm: ProductInput = {
  nombre: '',
  descripcion: '',
  precio: 0,
  imagenUrl: null,
  disponible: true,
  categoriaId: null,
}

export default function AdminProductoForm() {
  const { id } = useParams()
  const esEdicion = Boolean(id)
  const navigate = useNavigate()

  const [form, setForm] = useState<ProductInput>(initialForm)
  const [categorias, setCategorias] = useState<Categoria[]>([])
  const [subiendo, setSubiendo] = useState(false)
  const [guardando, setGuardando] = useState(false)
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    getCategorias().then(setCategorias)
  }, [])

  useEffect(() => {
    if (!id) return
    getProduct(Number(id)).then((p) =>
      setForm({
        nombre: p.nombre,
        descripcion: p.descripcion,
        precio: p.precio,
        imagenUrl: p.imagenUrl,
        disponible: p.disponible,
        categoriaId: p.categoriaId,
      }),
    )
  }, [id])

  const handleArchivo = async (e: ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0]
    if (!file) return
    setSubiendo(true)
    setError(null)
    try {
      const url = await uploadImagen(file)
      setForm((f) => ({ ...f, imagenUrl: url }))
    } catch {
      setError('No se pudo subir la imagen. Probá de nuevo.')
    } finally {
      setSubiendo(false)
    }
  }

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault()
    setGuardando(true)
    setError(null)
    try {
      if (esEdicion) {
        await updateProduct(Number(id), form)
      } else {
        await createProduct(form)
      }
      navigate('/admin/productos')
    } catch {
      setError('No se pudo guardar el producto.')
    } finally {
      setGuardando(false)
    }
  }

  return (
    <div className="max-w-xl">
      <h1 className="text-xl font-semibold text-mauve mb-6">
        {esEdicion ? 'Editar producto' : 'Nuevo producto'}
      </h1>

      <form onSubmit={handleSubmit} className="space-y-4 bg-white rounded-xl shadow-sm p-6">
        <div>
          <label className="block text-sm font-medium text-[#3D2C33] mb-1">Nombre</label>
          <input
            type="text"
            required
            value={form.nombre}
            onChange={(e) => setForm((f) => ({ ...f, nombre: e.target.value }))}
            className="w-full rounded-lg border border-taupe/50 px-3 py-2"
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-[#3D2C33] mb-1">Descripción</label>
          <textarea
            value={form.descripcion ?? ''}
            onChange={(e) => setForm((f) => ({ ...f, descripcion: e.target.value }))}
            className="w-full rounded-lg border border-taupe/50 px-3 py-2"
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-[#3D2C33] mb-1">Precio</label>
          <input
            type="number"
            step="0.01"
            min="0"
            required
            value={form.precio}
            onChange={(e) => setForm((f) => ({ ...f, precio: Number(e.target.value) }))}
            className="w-full rounded-lg border border-taupe/50 px-3 py-2"
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-[#3D2C33] mb-1">Categoría</label>
          <select
            value={form.categoriaId ?? ''}
            onChange={(e) =>
              setForm((f) => ({ ...f, categoriaId: e.target.value ? Number(e.target.value) : null }))
            }
            className="w-full rounded-lg border border-taupe/50 px-3 py-2"
          >
            <option value="">Sin categoría</option>
            {categorias.map((c) => (
              <option key={c.id} value={c.id}>
                {c.nombre}
              </option>
            ))}
          </select>
        </div>

        <div className="flex items-center gap-2">
          <input
            id="disponible"
            type="checkbox"
            checked={form.disponible}
            onChange={(e) => setForm((f) => ({ ...f, disponible: e.target.checked }))}
          />
          <label htmlFor="disponible" className="text-sm text-[#3D2C33]">
            Disponible
          </label>
        </div>

        <div>
          <label className="block text-sm font-medium text-[#3D2C33] mb-1">Foto</label>
          <input type="file" accept="image/jpeg,image/png,image/webp" onChange={handleArchivo} />
          {subiendo && <p className="text-sm text-taupe mt-1">Subiendo…</p>}
          {form.imagenUrl && (
            <img
              src={form.imagenUrl}
              alt="Vista previa"
              className="w-24 h-24 object-cover rounded-lg mt-2"
            />
          )}
        </div>

        {error && <p className="text-sm text-red-600">{error}</p>}

        <button
          type="submit"
          disabled={guardando || subiendo}
          className="rounded-full bg-rose-metallic hover:bg-rose-metallic-dark text-white font-medium px-6 py-2.5 transition-colors disabled:opacity-60"
        >
          {guardando ? 'Guardando…' : 'Guardar'}
        </button>
      </form>
    </div>
  )
}
```

- [ ] **Step 2: Verificar que compila**

Run: `cd frontend && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/admin/AdminProductoForm.tsx
git commit -m "Agrega formulario de alta/edición de producto"
```

---

### Task 22: AdminProductos (listado)

**Files:**
- Create: `frontend/src/pages/admin/AdminProductos.tsx`

**Interfaces:**
- Consumes: `getProducts` (existente), `deleteProduct` (Task 19).
- Produces: componente `AdminProductos`, usado en Task 23.

- [ ] **Step 1: Crear la página**

```typescript
// frontend/src/pages/admin/AdminProductos.tsx
import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { getProducts, deleteProduct } from '../../api/products'
import type { Product } from '../../types/product'

export default function AdminProductos() {
  const [productos, setProductos] = useState<Product[]>([])
  const [loading, setLoading] = useState(true)

  const cargar = () => {
    setLoading(true)
    getProducts()
      .then(setProductos)
      .finally(() => setLoading(false))
  }

  useEffect(() => {
    cargar()
  }, [])

  const handleEliminar = async (producto: Product) => {
    if (!confirm(`¿Seguro que querés eliminar "${producto.nombre}"?`)) return
    await deleteProduct(producto.id)
    cargar()
  }

  if (loading) return <p>Cargando productos…</p>

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-xl font-semibold text-mauve">Productos</h1>
        <Link
          to="/admin/productos/nuevo"
          className="rounded-full bg-rose-metallic hover:bg-rose-metallic-dark text-white text-sm font-medium px-4 py-2 transition-colors"
        >
          + Nuevo producto
        </Link>
      </div>

      <div className="bg-white rounded-xl shadow-sm overflow-hidden">
        <table className="w-full text-sm">
          <thead className="bg-cream text-left text-[#3D2C33]">
            <tr>
              <th className="p-3">Foto</th>
              <th className="p-3">Nombre</th>
              <th className="p-3">Categoría</th>
              <th className="p-3">Precio</th>
              <th className="p-3">Disponible</th>
              <th className="p-3"></th>
            </tr>
          </thead>
          <tbody>
            {productos.map((p) => (
              <tr key={p.id} className="border-t border-taupe/20">
                <td className="p-3">
                  {p.imagenUrl && (
                    <img
                      src={p.imagenUrl}
                      alt={p.nombre}
                      className="w-12 h-12 object-cover rounded-lg"
                    />
                  )}
                </td>
                <td className="p-3">{p.nombre}</td>
                <td className="p-3">{p.categoria?.nombre ?? '—'}</td>
                <td className="p-3">${p.precio.toFixed(2)}</td>
                <td className="p-3">{p.disponible ? 'Sí' : 'No'}</td>
                <td className="p-3 text-right space-x-3">
                  <Link to={`/admin/productos/${p.id}/editar`} className="text-mauve hover:underline">
                    Editar
                  </Link>
                  <button onClick={() => handleEliminar(p)} className="text-red-600 hover:underline">
                    Eliminar
                  </button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar que compila**

Run: `cd frontend && npx tsc -b --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/admin/AdminProductos.tsx
git commit -m "Agrega listado de productos del panel de admin"
```

---

### Task 23: Rutas /admin + wiring final

**Files:**
- Modify: `frontend/src/routes/index.tsx`
- Modify: `frontend/src/App.tsx`

**Interfaces:**
- Consumes: `AdminLogin` (Task 17), `ProtectedRoute` (Task 15), `AdminLayout` (Task 18), `AdminProductos` (Task 22), `AdminProductoForm` (Task 21), `AuthProvider` (Task 13), `GoogleOAuthProvider` (Task 16).

- [ ] **Step 1: Agregar las rutas de admin**

Reemplazar el contenido completo de `frontend/src/routes/index.tsx` por:

```typescript
import { Routes, Route } from 'react-router-dom'
import Home from '../pages/Home'
import Especialidades from '../pages/Especialidades'
import SobreMi from '../pages/SobreMi'
import Contacto from '../pages/Contacto'
import NotFound from '../pages/NotFound'
import AdminLogin from '../pages/admin/AdminLogin'
import AdminProductos from '../pages/admin/AdminProductos'
import AdminProductoForm from '../pages/admin/AdminProductoForm'
import ProtectedRoute from '../components/admin/ProtectedRoute'
import AdminLayout from '../components/admin/AdminLayout'

export default function AppRoutes() {
  return (
    <Routes>
      <Route path="/" element={<Home />} />
      <Route path="/especialidades" element={<Especialidades />} />
      <Route path="/sobre-mi" element={<SobreMi />} />
      <Route path="/contacto" element={<Contacto />} />

      <Route path="/admin/login" element={<AdminLogin />} />
      <Route element={<ProtectedRoute />}>
        <Route element={<AdminLayout />}>
          <Route path="/admin/productos" element={<AdminProductos />} />
          <Route path="/admin/productos/nuevo" element={<AdminProductoForm />} />
          <Route path="/admin/productos/:id/editar" element={<AdminProductoForm />} />
        </Route>
      </Route>

      <Route path="*" element={<NotFound />} />
    </Routes>
  )
}
```

- [ ] **Step 2: Envolver la app con `AuthProvider` y `GoogleOAuthProvider`**

Reemplazar el contenido de `frontend/src/App.tsx` por:

```typescript
import { BrowserRouter } from 'react-router-dom'
import { GoogleOAuthProvider } from '@react-oauth/google'
import AppRoutes from './routes'
import { CartProvider } from './context/CartContext'
import { AuthProvider } from './context/AuthContext'
import CartDrawer from './components/CartDrawer'

const googleClientId = import.meta.env.VITE_GOOGLE_CLIENT_ID as string

function App() {
  return (
    <GoogleOAuthProvider clientId={googleClientId}>
      <BrowserRouter>
        <AuthProvider>
          <CartProvider>
            <AppRoutes />
            <CartDrawer />
          </CartProvider>
        </AuthProvider>
      </BrowserRouter>
    </GoogleOAuthProvider>
  )
}

export default App
```

- [ ] **Step 3: Probar en el navegador**

```bash
cd frontend
npm run dev
```

Abrir `http://localhost:5173/admin/productos` en el navegador:
- Expected: redirige automáticamente a `/admin/login` (no hay sesión todavía).

En `/admin/login`, entrar con `admin-local@test.com` / `PruebaLocal123!` (la cuenta seedeada en la Task 3):
- Expected: redirige a `/admin/productos` y muestra la tabla con los 10 productos (con sus fotos, si ya se corrió la Task 12).

Probar crear un producto nuevo con foto, editarlo, y eliminarlo — confirmar que la tabla se actualiza en cada caso.

Cerrar sesión con el botón del header y confirmar que vuelve a pedir login.

(El botón "Iniciar sesión con Google" no va a funcionar todavía si `Google:ClientId` sigue vacío en el backend — pedirle al usuario que complete ese valor real en `appsettings.Development.json` y en Railway antes de probarlo, usando el Client ID generado en la Task 16.)

Parar el servidor (Ctrl+C).

- [ ] **Step 4: Commit**

```bash
git add frontend/src/routes/index.tsx frontend/src/App.tsx
git commit -m "Conecta las rutas del panel de admin y los providers de auth"
```

---

## Despliegue (después de que todas las tareas anteriores estén verificadas localmente)

- [ ] En Railway, agregar las variables de entorno: `Jwt__Secret` (generar una nueva, distinta a la local), `Google__ClientId`, `Supabase__S3__AccessKey`, `Supabase__S3__SecretKey`, `AdminSeed__0__Nombre`, `AdminSeed__0__Email`, `AdminSeed__0__Password`, `AdminSeed__1__Nombre`, `AdminSeed__1__Email`, `AdminSeed__1__Password` (usando los emails y contraseñas reales de c3sar64 y Sarah, no los de prueba).
- [ ] En Cloudflare Pages, agregar la variable de entorno `VITE_GOOGLE_CLIENT_ID` con el mismo Client ID, y agregar `https://web-sarah.pages.dev` a los orígenes autorizados en Google Cloud Console si no se hizo en la Task 16.
- [ ] Hacer `git push` de todos los commits de este plan.
- [ ] Verificar en producción: login con contraseña, login con Google, crear/editar/eliminar un producto de prueba con foto.

---

## Self-Review

**Cobertura de la spec:** autenticación por contraseña ✅ (Task 7), login con Google ✅ (Task 8), bloqueo a 3 intentos + auto-desbloqueo con Google ✅ (Tasks 7 y 8), protección de endpoints de productos ✅ (Task 9), subida a Supabase Storage con carpeta `fotos/productos` ✅ (Tasks 10, 11), migración de las 10 fotos existentes ✅ (Task 12), pantallas de login/listado/formulario ✅ (Tasks 17, 21, 22), rutas protegidas ✅ (Tasks 15, 23), manejo de errores descrito en la spec (mensajes genéricos, 401 → redirect, 413/415/502) ✅ (Tasks 7, 8, 11, 14).

**Placeholders:** ninguno — todos los pasos usan código completo y valores reales (incluyendo las credenciales S3 ya provistas, solo en el archivo gitignored).

**Consistencia de tipos:** `AuthResponse(Token, Nombre, Email)` se usa igual en Tasks 7, 8, 13, 17. `IStorageService.SubirImagenAsync(IFormFile, string)` se define en Task 10 y se usa igual en Task 11. `ProductInput` se define en Task 19 y se usa igual en Tasks 21 y 22. `useAuth()` se define en Task 13 y se consume igual en Tasks 15, 17, 18.
